{
  "name": "qr-inmuebles",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "db:push": "pnpm --filter api prisma db push",
    "db:seed": "pnpm --filter api db:seed"
  },
  "workspaces": [
    "packages/*"
  ]
}